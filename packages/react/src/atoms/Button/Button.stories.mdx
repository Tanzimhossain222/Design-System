import { Meta } from '@storybook/addon-docs/blocks';
import Button from './Button';

<Meta title='docs/atoms/Button' component= {Button} />

# Button Documanetation

In this section you will learn more about button component Button is an atoms in our design system. You can use it in your project.

## Usage

```javascript
<Button title= "Demo Button"  onClick= {() => alert('Button Clicked')} > Click Me </Button>
```